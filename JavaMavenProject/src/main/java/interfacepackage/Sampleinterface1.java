package interfacepackage;

public interface Sampleinterface1 {
             public void show();      //already abstract method;no need to give again abstract in method
                                        //check interface with variables
}
